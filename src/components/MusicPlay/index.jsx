import PropTypes from "prop-types";
import React, { memo } from "react";
import { MusicPlayWrapper } from "./style";
const MusicPlay = memo((props) => {
  return <MusicPlayWrapper>MusicPlay</MusicPlayWrapper>;
});

MusicPlay.propTypes = {};

export default MusicPlay;
